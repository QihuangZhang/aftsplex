% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fit.R
\name{predict_curve}
\alias{predict_curve}
\title{Predict the centred dose-response curve at a reference covariate setting}
\usage{
predict_curve(fit, x_grid, v_ref = NULL, x_var)
}
\arguments{
\item{fit}{A \code{survreg} object returned by \code{\link[=fit_aft_spline]{fit_aft_spline()}}.}

\item{x_grid}{Numeric vector of exposure values to predict at.}

\item{v_ref}{Named numeric vector of reference covariate values, with
names matching the \code{covariates} used when fitting. Pass \code{NULL} if the
model has no covariates.}

\item{x_var}{Name of the exposure column (must match \code{fit_aft_spline}).}
}
\value{
A numeric vector of length \code{length(x_grid)} (the uncentred linear
predictor; subtract \code{out[1]} to centre at the lower grid boundary).
}
\description{
Returns the linear predictor of a fitted \code{\link[=fit_aft_spline]{fit_aft_spline()}} model on an
exposure grid, holding the covariates fixed at \code{v_ref}. Subtracting the
first element produces the centred curve used throughout the manuscript.
}
\examples{
sim <- generate_aft_data(n = 300, n_val = 100, seed = 1)
fit <- fit_aft_spline(sim$survival, x_var = "X_true",
                      covariates = c("V1","V2","V3","V4"))
x_grid <- seq(7, 13, length.out = 50)
lp <- predict_curve(fit, x_grid,
                    v_ref = c(V1=30, V2=30, V3=0, V4=0),
                    x_var = "X_true")

}
