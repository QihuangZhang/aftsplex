% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/viz.R
\name{plot_curves}
\alias{plot_curves}
\title{Overlay fitted dose-response curves with the truth and an optional CI}
\usage{
plot_curves(
  x_grid,
  truth,
  fits,
  ci = NULL,
  title = "AFT + spline dose-response",
  x_lab = "Exposure",
  y_lab = "Centred linear predictor (log time ratio)",
  colors = method_colors()
)
}
\arguments{
\item{x_grid}{Numeric exposure grid.}

\item{truth}{Numeric vector of truth values (already centred at the
first grid point), the same length as \code{x_grid}.}

\item{fits}{Named list of numeric vectors of length \code{length(x_grid)}.}

\item{ci}{Optional list with elements \code{lower} and \code{upper}, each a
numeric vector of length \code{length(x_grid)}.}

\item{title}{Plot title.}

\item{x_lab, y_lab}{Axis labels.}

\item{colors}{Optional named character vector of colours; defaults to
\code{\link[=method_colors]{method_colors()}} (matching \code{Naive}, \code{Oracle}, \code{SIMEX}).}
}
\value{
A \code{ggplot} object.
}
\description{
Builds a faceted ggplot2 figure comparing a named list of fitted curves
against the truth, with an optional bootstrap confidence ribbon. Uses
the \code{\link[=method_colors]{method_colors()}} palette by default.
}
\details{
Requires the suggested package \code{ggplot2} to be installed.
}
\examples{
if (requireNamespace("ggplot2", quietly = TRUE)) {
  x <- seq(6, 14, length.out = 50)
  truth <- f_true(x) - f_true(x[1])
  plot_curves(x, truth, list(Oracle = truth))
}

}
