% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simex.R
\name{simex_aft_spline}
\alias{simex_aft_spline}
\title{SIMEX-correct the AFT-spline dose-response curve}
\usage{
simex_aft_spline(
  data,
  x_var,
  sigma_w_sq,
  covariates = character(0),
  v_ref = NULL,
  df = 4,
  knots = NULL,
  outcome_var = "T_obs",
  status_var = "delta",
  lambda = c(0.5, 1, 1.5, 2),
  B = 50,
  dist = "lognormal",
  x_grid = NULL,
  x_ref = NULL,
  support_probs = NULL,
  verbose = FALSE,
  maxiter = 200
)
}
\arguments{
\item{data}{A data frame containing the columns named in \code{x_var},
\code{covariates}, \code{outcome_var}, and \code{status_var}.}

\item{x_var}{Name of the exposure column.}

\item{sigma_w_sq}{Scalar conditional variance of the calibrated exposure
(typically \code{gls_combine(...)$sigma_w_sq}).}

\item{covariates}{Character vector of confounder column names.}

\item{v_ref}{Named numeric vector of reference covariate values matching
\code{covariates}. \code{NULL} if no covariates.}

\item{df, knots, dist}{Passed to \code{\link[=fit_aft_spline]{fit_aft_spline()}}.}

\item{outcome_var, status_var}{Passed to \code{\link[=fit_aft_spline]{fit_aft_spline()}}.}

\item{lambda}{Numeric grid of SIMEX lambda values (excluding 0; 0 is
appended internally as the naive fit). Default \code{c(0.5, 1, 1.5, 2)}.}

\item{B}{Number of inner replicates per lambda.}

\item{x_grid}{Numeric grid of exposure values at which to evaluate the
corrected curve. Defaults to 100 equally spaced points spanning
\code{data[[x_var]]}.}

\item{x_ref}{Optional scalar reference exposure at which to anchor the
centred curves (so the dose-response is read as relative to \code{x_ref},
e.g. a clinically meaningful value). \code{NULL} (default) anchors at the
lower grid boundary \code{x_grid[1]}, preserving the original behaviour.}

\item{support_probs}{Optional length-2 numeric of probabilities defining
the trustworthy exposure support as quantiles of \code{data[[x_var]]} (e.g.
\code{c(0.05, 0.95)}). When set, grid points outside the support are flagged
in the returned \code{in_support} vector and a one-shot \code{warning()} notes
that they are spline extrapolations. \code{NULL} (default) disables the check.}

\item{verbose}{Print progress bar and non-convergence count?}

\item{maxiter}{\code{survreg} iteration cap (passed via
\code{\link[survival:survreg.control]{survival::survreg.control()}}).}
}
\value{
An object of class \code{"simex_aft_spline"}: a list with elements
\itemize{
\item \code{x_grid} the exposure grid,
\item \code{curve_simex} the centred SIMEX-corrected linear predictor,
\item \code{curve_naive} the centred naive linear predictor (lambda = 0),
\item \code{curves_lambda} matrix of centred lambda-fits (columns = lambda, including 0),
\item \code{lambda} the lambda values used (with 0 in column 1),
\item \code{in_support} logical vector aligned to \code{x_grid} (or \code{NULL} if
\code{support_probs} was not supplied),
\item \code{x_ref} the reference anchor used (or \code{NULL}),
\item \code{call}, \code{n}, \code{df}, \code{dist}, \code{covariates}, \code{B}, \code{sigma_w_sq}, \code{n_fail}
configuration and diagnostics used by \code{\link[=summary.simex_aft_spline]{summary.simex_aft_spline()}}
and \code{\link[=plot.simex_aft_spline]{plot.simex_aft_spline()}}.
}
}
\description{
Implements simulation-extrapolation (Cook and Stefanski 1994; Carroll,
Kuchenhoff, Lombard and Stefanski 1996) for a natural-cubic-spline AFT
model fitted by \code{\link[=fit_aft_spline]{fit_aft_spline()}}. For each \code{lambda} in the supplied
grid, draws \code{B} perturbed surrogates with additional noise
\code{sqrt(lambda * sigma_w_sq) * N(0, 1)}, refits, averages the centred
linear predictor across \code{B}, then extrapolates to \code{lambda = -1} via a
pointwise quadratic OLS.
}
\details{
Inner \code{survreg} fits are wrapped in convergence handling: fits that
fail to converge within \code{maxiter} iterations are dropped from the
average via \code{na.rm = TRUE}, and the count is reported if \code{verbose = TRUE}.
}
\examples{
\donttest{
sim <- generate_aft_data(n = 500, n_val = 200, seed = 1)
cal <- fit_me_calibration(sim$validation)
W   <- as.matrix(sim$survival[, cal$W_cols])
g   <- gls_combine(W, cal)
dat <- sim$survival
dat$W_bar <- g$W_bar
s <- simex_aft_spline(dat, x_var = "W_bar",
                      sigma_w_sq = g$sigma_w_sq,
                      covariates = c("V1","V2","V3","V4"),
                      v_ref = c(V1=30, V2=30, V3=0, V4=0),
                      lambda = c(0.5, 1, 1.5, 2), B = 10)
summary(s)
}

}
