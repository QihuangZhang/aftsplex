% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dgp.R
\name{f_true}
\alias{f_true}
\title{True dose-response used by the simulation DGP}
\usage{
f_true(x, beta = 0.6, alpha = 0.4, x_min = 6)
}
\arguments{
\item{x}{Numeric vector of exposure values.}

\item{beta}{Saturation amplitude (asymptote as \code{x} grows large).}

\item{alpha}{Curvature parameter (larger = steeper initial rise).}

\item{x_min}{Threshold below which the response is flat at zero.}
}
\value{
Numeric vector the same length as \code{x}.
}
\description{
Saturating exponential \code{beta * (1 - exp(-alpha * pmax(x - x_min, 0)))}.
This is the function the simulation tries to recover; it serves as the
ground truth in \code{\link[=generate_aft_data]{generate_aft_data()}} and is exported so users can
overlay it on plots.
}
\examples{
x <- seq(0, 20, length.out = 100)
plot(x, f_true(x), type = "l")

}
