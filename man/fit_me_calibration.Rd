% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/phase1.R
\name{fit_me_calibration}
\alias{fit_me_calibration}
\title{Fit the Phase-1 multivariate ME calibration model}
\usage{
fit_me_calibration(
  validation,
  x_var = "X_true",
  surrogate_pattern = "^W[0-9]+$"
)
}
\arguments{
\item{validation}{Validation data frame containing the truth column named
in \code{x_var} and the surrogate columns matched by \code{surrogate_pattern}.}

\item{x_var}{Name of the truth column. Default \code{"X_true"}.}

\item{surrogate_pattern}{Regex used to identify surrogate columns; see
\code{\link[=extract_surrogate_cols]{extract_surrogate_cols()}}. Default \code{"^W[0-9]+$"}.}
}
\value{
A list with elements
\itemize{
\item \code{alpha0} numeric vector of intercepts (length \code{J}),
\item \code{alpha1} numeric vector of slopes (length \code{J}),
\item \code{Sigma_e} \verb{J x J} residual covariance,
\item \code{W_cols} character vector of surrogate column names used,
\item \code{n_val} number of validation rows.
}
}
\description{
Fits per-equation OLS of each surrogate column on the validation truth:
\code{W_j = alpha_0j + alpha_1j * X + e_j}, with \code{e ~ N_J(0, Sigma_e)}.
Under shared regressors this is equivalent to seemingly-unrelated
regression (SUR) and admits closed-form ML.
}
\examples{
sim <- generate_aft_data(n = 100, n_val = 200, seed = 1)
fit_me_calibration(sim$validation)

}
