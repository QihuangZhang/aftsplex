% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bootstrap.R
\name{two_stage_bootstrap}
\alias{two_stage_bootstrap}
\title{Two-stage nonparametric bootstrap for the SIMEX-corrected curve}
\usage{
two_stage_bootstrap(
  survival,
  validation,
  x_var = "X_true",
  covariates = character(0),
  v_ref = NULL,
  surrogate_pattern = "^W[0-9]+$",
  df = 4,
  knots = NULL,
  outcome_var = "T_obs",
  status_var = "delta",
  lambda = c(0.5, 1, 1.5, 2),
  B = 50,
  R = 200,
  x_grid = NULL,
  nested = TRUE,
  x_ref = NULL,
  support_probs = NULL,
  workers = 1L,
  dist = "lognormal",
  verbose = FALSE
)
}
\arguments{
\item{survival}{Main-study data frame; must contain the columns named in
\code{x_var} (if a single calibrated exposure already exists), or the
surrogate columns matched by \code{surrogate_pattern}, plus the outcome,
status, and covariate columns.}

\item{validation}{Validation data frame containing the truth column
named in \code{x_var} and the surrogate columns matched by
\code{surrogate_pattern}.}

\item{x_var}{Name of the exposure column. For simulation use the truth
column name (\code{"X_true"}); for the standard workflow this is the same
name used in both \code{validation} and the bootstrap-recombined
\code{survival} data.}

\item{covariates}{Character vector of confounder column names.}

\item{v_ref}{Named numeric vector of reference covariate values matching
\code{covariates}. \code{NULL} if no covariates.}

\item{surrogate_pattern}{Regex used to identify surrogate columns;
default \code{"^W[0-9]+$"}.}

\item{df, knots, lambda, B, dist}{Passed to \code{\link[=simex_aft_spline]{simex_aft_spline()}}.}

\item{outcome_var, status_var}{Passed to \code{\link[=simex_aft_spline]{simex_aft_spline()}}.}

\item{R}{Number of outer bootstrap replicates.}

\item{x_grid}{Numeric exposure grid for the curve. If \code{NULL}, taken as
100 equally spaced points between the 2nd and 98th percentile of the
full-sample combined surrogate.}

\item{nested}{Logical; if \code{TRUE} (default) run the full two-stage
bootstrap that resamples the validation sample and refits the Phase-1
calibration on every replicate. If \code{FALSE}, hold the calibration fixed
at the full-sample fit and resample only the main study (single-stage),
giving the conditional-variance interval. Running both and comparing
\code{mean(upper - lower)} quantifies the Phase-1 contribution to the width.}

\item{x_ref}{Optional scalar reference exposure at which to anchor the
curves (and every bootstrap replicate) before taking quantiles, so the
interval is read relative to \code{x_ref}. \code{NULL} (default) anchors at
\code{x_grid[1]}.}

\item{support_probs}{Optional length-2 probabilities defining the
trustworthy exposure support as quantiles of the full-sample combined
surrogate. When set, an \code{in_support} vector is returned and a one-shot
\code{warning()} flags out-of-support grid points. \code{NULL} (default) disables.}

\item{workers}{Integer number of parallel workers for the outer bootstrap
loop. \code{1L} (default) runs serially and is the canonical reproducible path:
it draws from R's global RNG stream, so a given \code{set.seed()} gives
bit-for-bit identical results. When \verb{> 1}, replicates run via
\code{future.apply::future_lapply()} on a \code{multisession} plan with per-task
L'Ecuyer streams (\code{future.seed = TRUE}); these are reproducible and
identical \emph{across worker counts} (e.g. \code{workers = 2} matches \code{workers = 3}),
but use independent streams and therefore \strong{differ from the serial
(\code{workers = 1}) result for the same seed} by design. Requires the \code{future}
and \code{future.apply} packages.}

\item{verbose}{Print progress bar? (Serial path only.)}
}
\value{
A list with elements
\itemize{
\item \code{x_grid} exposure grid,
\item \code{f_hat} single-fit SIMEX point estimate (full-sample, no resampling),
\item \code{bs_mean} mean of the bootstrap curves,
\item \code{curves} matrix of bootstrap curves (\code{length(x_grid)} rows by \code{R} cols),
\item \verb{median, lower, upper} percentile-CI summaries (2.5\%, 50\%, 97.5\%),
\item \verb{lower_basic, upper_basic} reverse-percentile (basic) CI bounds,
\item \code{f_bc} bias-corrected point estimate \code{2*f_hat - bs_mean},
\item \verb{sigma_w_sq_boot, omega_boot} per-replicate Phase-1 summaries,
\item \code{in_support} logical support flag (or \code{NULL}),
\item \code{R_effective} number of replicates that produced a curve.
}

The percentile CI is the primary interval; the basic (reverse-percentile)
CI is kept for completeness but does not improve coverage on simulated
diminishing-returns curves because the dominant residual bias is from
spline smoothing rather than SIMEX.
}
\description{
For each replicate \code{r}:
\enumerate{
\item resample validation participants;
\item refit the multivariate ME calibration via \code{\link[=fit_me_calibration]{fit_me_calibration()}};
\item resample main-study participants;
\item form the bootstrap-combined surrogate via \code{\link[=gls_combine]{gls_combine()}} under the
refit calibration;
\item run \code{\link[=simex_aft_spline]{simex_aft_spline()}} with the refit \code{sigma_w_sq} and store the
corrected curve.
}
}
\details{
Pointwise quantiles across \code{r} give CIs that propagate both Phase-1 and
Phase-2 uncertainty - this addresses the plug-in-variance critique that
the conditional \code{sigma_w_sq} alone yields anti-conservative intervals
when the validation sample is small relative to the main study.
}
\examples{
\donttest{
sim <- generate_aft_data(n = 500, n_val = 200, seed = 1)
boot <- two_stage_bootstrap(
  survival   = sim$survival,
  validation = sim$validation,
  x_var      = "X_true",
  covariates = c("V1","V2","V3","V4"),
  v_ref      = c(V1=30, V2=30, V3=0, V4=0),
  lambda     = c(0.5, 1, 1.5, 2), B = 5, R = 10
)
head(boot$f_hat)

# Phase-1 contribution to CI width: compare double vs single-stage.
single <- two_stage_bootstrap(
  survival = sim$survival, validation = sim$validation, x_var = "X_true",
  covariates = c("V1","V2","V3","V4"), v_ref = c(V1=30, V2=30, V3=0, V4=0),
  lambda = c(0.5, 1, 1.5, 2), B = 5, R = 10, nested = FALSE
)
mean(boot$upper - boot$lower) / mean(single$upper - single$lower)
}

}
