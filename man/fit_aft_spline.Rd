% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fit.R
\name{fit_aft_spline}
\alias{fit_aft_spline}
\title{Fit an AFT model with a natural-cubic-spline exposure}
\usage{
fit_aft_spline(
  data,
  x_var,
  covariates = character(0),
  df = 4,
  knots = NULL,
  outcome_var = "T_obs",
  status_var = "delta",
  dist = "lognormal",
  control = NULL
)
}
\arguments{
\item{data}{A data frame containing the outcome, status, exposure, and
covariate columns named below.}

\item{x_var}{Name of the exposure column.}

\item{covariates}{Character vector of additional covariate column names.
Pass \code{character(0)} (the default) for an exposure-only model.}

\item{df}{Spline degrees of freedom (ignored if \code{knots} is supplied).}

\item{knots}{Optional numeric vector of interior knot locations.}

\item{outcome_var}{Name of the time-to-event column. Default \code{"T_obs"}.}

\item{status_var}{Name of the event indicator column. Default \code{"delta"}.}

\item{dist}{Parametric AFT error distribution, passed directly to the
\code{dist} argument of \code{\link[survival:survreg]{survival::survreg()}}. Any distribution \code{survreg}
accepts is valid here, including \code{"weibull"}, \code{"exponential"},
\code{"loglogistic"}, \code{"lognormal"}, and \code{"gaussian"}; see
\code{\link[survival:survreg]{survival::survreg()}} for the full, authoritative list. The default
\code{"lognormal"} matches the DGP in \code{\link[=generate_aft_data]{generate_aft_data()}}. Select another
family to fit your data under, or to run a sensitivity analysis against,
an alternative AFT distribution.}

\item{control}{Optional \code{\link[survival:survreg.control]{survival::survreg.control()}} object.}
}
\value{
A \code{survreg} fitted-model object.
}
\description{
Thin wrapper around \code{\link[survival:survreg]{survival::survreg()}} that builds a formula of the
form \code{Surv(outcome_var, status_var) ~ ns(x_var, df = ...) + covariates}
from the column names supplied. Used three ways in the simulation: as the
oracle (on the true exposure), as the naive estimator (on the GLS-combined
surrogate without ME correction), and as the inner workhorse of
\code{\link[=simex_aft_spline]{simex_aft_spline()}}.
}
\examples{
sim <- generate_aft_data(n = 200, n_val = 100, seed = 1)
fit <- fit_aft_spline(sim$survival, x_var = "X_true",
                      covariates = c("V1","V2","V3","V4"))
coef(fit)

}
