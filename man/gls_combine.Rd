% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/phase1.R
\name{gls_combine}
\alias{gls_combine}
\title{GLS-combine surrogate columns into a single calibrated exposure}
\usage{
gls_combine(W, calibration)
}
\arguments{
\item{W}{Numeric matrix of surrogate columns, columns ordered as in
\code{calibration$W_cols}.}

\item{calibration}{Output of \code{\link[=fit_me_calibration]{fit_me_calibration()}}.}
}
\value{
A list with elements
\itemize{
\item \code{W_bar} numeric vector of length \code{nrow(W)},
\item \code{sigma_w_sq} scalar conditional variance,
\item \code{omega} numeric vector of GLS weights (length \code{J}),
\item \code{Sigma_tilde} \verb{J x J} back-transformed error covariance.
}
}
\description{
Applies the linear back-transform \code{W_tilde = (W - alpha_0) / alpha_1} and
the GLS combiner \verb{omega = Sigma_tilde^-1 1 / (1' Sigma_tilde^-1 1)} to
produce a single calibrated exposure with conditional variance
\verb{sigma_w_sq = (1' Sigma_tilde^-1 1)^-1}.
}
\examples{
sim <- generate_aft_data(n = 100, n_val = 200, seed = 1)
cal <- fit_me_calibration(sim$validation)
W   <- as.matrix(sim$survival[, cal$W_cols])
out <- gls_combine(W, cal)
head(out$W_bar)

}
