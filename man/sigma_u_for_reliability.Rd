% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dgp.R
\name{sigma_u_for_reliability}
\alias{sigma_u_for_reliability}
\title{Build a measurement-error covariance targeting a given reliability}
\usage{
sigma_u_for_reliability(reliability, var_x, ratios = c(2, 2.5, 3), rho = 0.4)
}
\arguments{
\item{reliability}{Target mean per-surrogate reliability in \verb{(0, 1)}.}

\item{var_x}{Variance of the latent exposure \code{X} (e.g. \code{x_sd^2} passed to
\code{\link[=generate_aft_data]{generate_aft_data()}}).}

\item{ratios}{Relative marginal error variances across the surrogates;
their scale is solved for, only their ratios matter. Default
\code{c(2.0, 2.5, 3.0)} matches \code{\link[=build_sigma_u]{build_sigma_u()}}.}

\item{rho}{Common pairwise error correlation, passed to \code{\link[=build_sigma_u]{build_sigma_u()}}.}
}
\value{
A \verb{J x J} covariance matrix suitable for the \code{Sigma_u} argument of
\code{\link[=generate_aft_data]{generate_aft_data()}}.
}
\description{
Returns a \code{Sigma_u} (via \code{\link[=build_sigma_u]{build_sigma_u()}}) whose surrogate-measurement
error variances are scaled so the \strong{mean per-surrogate reliability} equals
\code{reliability}. Surrogate reliability is the intraclass-style ratio
\code{var_x / (var_x + var_U_j)}; the relative surrogate qualities in \code{ratios}
are preserved (so the surrogates stay unequal for the per-surrogate ladder),
and a single multiplicative scale is solved by \code{\link[stats:uniroot]{stats::uniroot()}} to hit the
target average. Use it to sweep measurement quality in a simulation, e.g.
\code{reliability} in \code{c(0.5, 0.65, 0.8)}.
}
\examples{
S <- sigma_u_for_reliability(0.65, var_x = 75^2)
# mean per-surrogate reliability is ~0.65:
mean(75^2 / (75^2 + diag(S)))

}
