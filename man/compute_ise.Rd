% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/eval.R
\name{compute_ise}
\alias{compute_ise}
\title{Integrated squared error of a fitted curve against the truth}
\usage{
compute_ise(x_grid, truth_fn, fits)
}
\arguments{
\item{x_grid}{Numeric vector of exposure values.}

\item{truth_fn}{A function such as \code{\link[=f_true]{f_true()}} returning the (uncentred)
true response at \code{x_grid}.}

\item{fits}{A named list of numeric vectors, each of length
\code{length(x_grid)}, holding fitted centred curves.}
}
\value{
A list with elements \code{truth} (the centred truth at \code{x_grid}) and
\code{ise} (named numeric vector of ISEs, one entry per element of \code{fits}).
}
\description{
Trapezoidal-rule approximation of \verb{integral of (f_hat - f_true)^2 dx}
over the exposure grid. The truth function is centred at the lower grid
boundary to match the convention used elsewhere in the package
(\code{truth(x_grid) - truth(x_grid[1])}).
}
\examples{
x_grid <- seq(6, 14, length.out = 50)
fits <- list(naive = numeric(length(x_grid)),
             oracle = f_true(x_grid) - f_true(x_grid[1]))
compute_ise(x_grid, f_true, fits)

}
