% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simex.R
\name{plot.simex_aft_spline}
\alias{plot.simex_aft_spline}
\title{Plot a SIMEX-corrected AFT-spline fit}
\usage{
\method{plot}{simex_aft_spline}(
  x,
  truth = NULL,
  ci = NULL,
  show_naive = TRUE,
  title = "SIMEX-corrected AFT-spline dose-response",
  ...
)
}
\arguments{
\item{x}{A \code{\link[=simex_aft_spline]{simex_aft_spline()}} fit.}

\item{truth}{Optional numeric vector (length \code{length(x$x_grid)}) of the
centred truth curve to overlay as a dashed black line.}

\item{ci}{Optional list with elements \code{lower} and \code{upper}.}

\item{show_naive}{Logical; overlay the naive (\code{lambda = 0}) curve?
Default \code{TRUE}.}

\item{title}{Plot title.}

\item{...}{Passed through to \code{\link[=plot_curves]{plot_curves()}}.}
}
\value{
A \code{ggplot} object.
}
\description{
Convenience S3 method that draws the SIMEX-corrected curve via
\code{\link[=plot_curves]{plot_curves()}}. By default the naive curve (\code{lambda = 0}) is overlaid
as a smoothing-bias diagnostic. Pass \code{truth} to add the dashed truth
line, and \code{ci} to add a percentile ribbon (typically from
\code{\link[=two_stage_bootstrap]{two_stage_bootstrap()}}).
}
