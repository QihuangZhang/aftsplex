% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dgp.R
\name{generate_aft_data}
\alias{generate_aft_data}
\title{Generate one Monte Carlo replicate of the AFT-spline-SIMEX scenario}
\usage{
generate_aft_data(
  n = 2000,
  n_val = 500,
  Sigma_u = build_sigma_u(),
  mu = 4,
  sigma_T = 0.8,
  gamma = c(0.1, -0.05, 0.3, -0.2),
  cens_rate = 5e-04,
  seed = NULL
)
}
\arguments{
\item{n}{Main-study sample size.}

\item{n_val}{Validation sample size.}

\item{Sigma_u}{\verb{J x J} measurement-error covariance (default 3x3 with
marginal variances \verb{(2.0, 2.5, 3.0)} and \code{rho = 0.4}).}

\item{mu}{AFT intercept.}

\item{sigma_T}{AFT log-scale standard deviation.}

\item{gamma}{Numeric vector of length 4: confounder coefficients in the
linear predictor (\verb{V1, V2, V3, V4}).}

\item{cens_rate}{Rate of the exponential censoring distribution. Smaller
values give heavier event experience. The default \code{0.0005} yields
~76\% events under the other defaults.}

\item{seed}{Optional integer seed.}
}
\value{
A list with elements \code{survival} (main-study data frame),
\code{validation} (validation data frame with paired truth + surrogates),
and \code{truth} (a list of the parameter values used).
}
\description{
Produces a main-study survival sample and an external validation sample
from the same data-generating process described in the manuscript
appendix. The latent log-exposure is normal, the dose-response is the
saturating curve \code{\link[=f_true]{f_true()}}, the survival times follow a log-normal AFT
model with four confounders (V1-V4), and the surrogate measurements are
multivariate-normal around the latent truth with covariance \code{Sigma_u}.
}
\details{
The returned \code{survival} data frame is in the shape expected by
\code{\link[=fit_aft_spline]{fit_aft_spline()}} and \code{\link[=two_stage_bootstrap]{two_stage_bootstrap()}} with their defaults
(\code{outcome_var = "T_obs"}, \code{status_var = "delta"},
\code{covariates = c("V1", "V2", "V3", "V4")},
\code{surrogate_cols = NULL} -> auto-detect via \code{\link[=extract_surrogate_cols]{extract_surrogate_cols()}}).
}
\examples{
sim <- generate_aft_data(n = 200, n_val = 100, seed = 1)
names(sim)
head(sim$survival)

}
